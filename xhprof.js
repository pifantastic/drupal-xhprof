if (Drupal.jsEnabled) {
  $(function() {
    /*
    $("table#xhprof-runs-table tr").bind('mouseenter', function() {
      $tr = $(this);
      $trOptions = $('<tr colspan="4"><td>Options</td></tr>');
      $tr.after($trOptions);
    });

    $("table#xhprof-runs-table tr").bind('mouseleave', function() {
      $(this).next().remove();
    });
    */
  });
}
